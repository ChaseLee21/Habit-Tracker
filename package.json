{
  "name": "habit-tracker",
  "version": "1.0.0",
  "description": "This project is a passion project that was inspired by the book \"Atomic Habits\" by James Clear. I am in no way affiliated with nor sponsor the author or any company he is associated with.",
  "main": "index.js",
  "scripts": {
    "start": "cd server && node server.js",
    "dev": "concurrently \"cd server && node server.js\" \"cd client && npm run dev\"",
    "build": "npm install && cd client && npm install && cd ../server && npm install && cd ../client && npm run build"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "dotenv": "^16.4.5"
  }
}
